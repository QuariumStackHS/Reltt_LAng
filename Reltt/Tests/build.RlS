Begin:

using Builds
> setup
> Set -> help.RlS help
> Build-this
> Call -> help

show

if ( name == "help_exe" )
    cout "Help"
endif;
else if ( name == "help_exe" )
    cout "Helper"
endif;
else
    cout "helse"
endif;
*/
end;