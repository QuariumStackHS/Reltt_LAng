Begin:

using Builds
> setup
> Set -> help.RlS help
> Build-this
> Call -> help

show

var argv-0 ( argv-0 + "Loler" )
if ( name == "help_exe" )
    cout "Help"
    if ( name != "argv-0" )
        cout "is not argv-0"
    else if ( name != "argv-0" )
        cout "is maybe argv-0"
    endif;
else if ( argv-0 == "Loler" )
    cout argv-0
else
    cout "helse"
endif;

end;