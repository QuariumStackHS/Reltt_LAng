Begin:

using Builds
> setup
> Set -> help.RlS help
> Build-this
> Call -> help

show

if ( name != "help_exe" ) then:
    cout "Help ----"

    /*if ( "helper" == "helper" ) then:
        cout "ypp ------"
    //elif ( name != "helper" )
        cout "y0 -----"
    else
    endif;*/
elif ( argv- != "Loler" ) then:
    cout "argv ----"
else
cout "None"
endif;

end;