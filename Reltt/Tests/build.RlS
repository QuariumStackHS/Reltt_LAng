Begin:

using Builds
> setup
> Set -> help.RlS help
> Build-this
> Call -> help

show

if ( name == "help_exe" )
    cout "Help"
endif;

var argv-0 ( argv-0 + "Loler" )
else if ( argv-0 != "Loler" )
    cout argv-0
endif;
else
    cout "helse"
endif;

end;